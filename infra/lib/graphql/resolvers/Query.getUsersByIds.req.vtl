{
  "version": "2017-02-28",
  "operation": "BatchGetItem",
  "tables": {
    "neighborly-users": {
      "keys": [
        #foreach($userId in $ctx.args.userIds)
          {
            "userId": $util.dynamodb.toDynamoDBJson($userId)
          }#if($foreach.hasNext),#end
        #end
      ],
      "consistentRead": false
    }
  }
}
